# PostgREST configuration — YourStockForecast
# SECURITY NOTE: contains DB password. Do NOT commit to git.
# ─────────────────────────────────────────────────────────────

# Key=value format avoids URI password-encoding issues (e.g. slashes in password)
db-uri          = "host=localhost port=5432 dbname=traderdude user=authenticator password=rqtz9Bqc5csXyzf/QGt4Zkujjd6iNtK1"

# Only expose public schema — auth schema not needed for the site
db-schemas      = "public"
db-anon-role    = "anon"

# Server
server-host     = "localhost"
server-port     = 3000

# JWT
jwt-secret      = "3f69bbd6f667cad0df0bd1753c3b733b942dfb6ad549bdc2b146f68358efe96b"
jwt-aud         = "postgrest"

# Performance
db-max-rows     = 1000
db-pool         = 10
db-pool-timeout = 10

server-timing-enabled = true
